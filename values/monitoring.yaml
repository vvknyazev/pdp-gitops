grafana:
  service:
    type: NodePort
    nodePort: 30300

prometheus:
  service:
    type: NodePort
    nodePort: 30090

  prometheusSpec:
    enableRemoteWriteReceiver: true

nodeExporter:
  enabled: false
